name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    uses: arikchakma/workflows/.github/workflows/release-pnpm.yml@v1
    with:
      publish: true
      build: pnpm run build
    permissions:
      contents: write
      id-token: write
    secrets: inherit
