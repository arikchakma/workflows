name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write
  id-token: write

jobs:
  release:
    uses: arikchakma/workflows/.github/workflows/release-bun.yml@v1
    with:
      publish: true
      build: bun run build
    secrets: inherit
